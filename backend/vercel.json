{
  "version": 2,
  "builds": [
    { "src": "api/**/*.js", "use": "@vercel/node" } // Adjust if your files are in an api folder
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}
